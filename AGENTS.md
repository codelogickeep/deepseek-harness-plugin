# AGENTS.md

本文件是给在此目录工作的 AI 编码代理的持久化工作约定（由 2026-08 会话沉淀）。

## 协作模式：pro 指挥 + flash 执行

当前默认 agent-preset 为 flash-worker：主 agent（deepseek-v4-pro）负责规划与关键链路，
flash 模型子 agent（通过 flash_agent 工具）负责执行具体子任务。

## 委派原则

### 主动委派给 flash（可并行、结果独立的调研/执行）
- 阅读多个依赖库源码并总结行为（例如读 SDK / node_modules 里的实现）
- 多文件审计、扫描、信息收集
- 独立、边界清晰的编码任务

### 自己串行完成（不委派）
- 强上下文依赖的根因定位与修复（分析结论直接决定后续每一步修改）
- 单文件、多处相互关联的小编辑
- 需要频繁对照多处上下文才能做对的小任务

## 经验教训
- flash_agent 不共享主对话上下文，委托前必须把完整背景打包进 prompt。
- 拆分任务时优先找"结果独立、可并行"的切面，而不是把串行链路硬拆。
